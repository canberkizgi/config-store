spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloudDB?createDatabaseIfNotExist=true
    username: root
    password: can123
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl